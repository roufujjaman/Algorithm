#include<bits/stdc++.h>
using namespace std;
const int N = 1e3+5;
vector<int> adj[N];
int main()
{
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++)
    {
        
    }  
    for (int i = 0; i < 4; i++)
    {
        cout << arr[i][3-i];
        cout << '\n';
    }
    return 0;
}