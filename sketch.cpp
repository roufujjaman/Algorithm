#include<bits/stdc++.h>
using namespace std;
vector<int> ss;
void fn(int n, int arr[])
{
    if (n == 0)
    {
        ss.push_back(NULL);
        return;
    }
}
int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++) cin >> arr[i];
    for (int i = 0; i < ss.size(); i++) cout << ss[i];
    return 0;
}